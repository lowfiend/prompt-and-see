# prompt-and-see

## Language

**場景檔**：
`behaviors/<slug>/*.feature`——目前需求的行為描述，全 repo 唯一手寫的行為來源；地圖與下游文件由它重生，MVP 依它增量實作。它不是整個 app 既有行為的總帳，場景沒寫到的既有功能不因此刪除。
_Avoid_: 規格、spec（那是 `/to-spec` 的生成文件）、需求文件、行為真相（舊名）

**行為地圖**：
從場景檔整檔重生的 Mermaid 圖（`map.md`）：畫面＝節點、失敗標紅、圖不比場景多。給使用者看懂流程、找缺漏用；不承載狀態、版本或決策紀錄。
_Avoid_: 流程圖、線框圖

**MVP**：
`behaviors/mvp/`（位置固定，入口固定是 `index.html`）——免安裝、免編譯、直接打開就能操作的靜態 HTML 實作。它是 code：有既有內容就原地增量修改、無關功能保留，人與 agent 都可維護，歷史交給 Git。
_Avoid_: 整合原型（舊名）、線框、mockup

**生成文件**：
`map.md`、`spec/` 由當前場景檔整檔重生（spec 的技術決定與不做什麼兩節例外：以訪談定案為準，重生時沿用）；`handoff.md` 依目前場景、MVP 與已知事項重新生成。皆手改無效、過期就重生。MVP code 不在此列。
_Avoid_: 產物（舊名，範圍曾誤含 MVP）

**需求資料夾**：
`behaviors/<slug>/`——一個需求的場景檔與生成文件住的地方。slug 用領域語言取名；`config`、`mvp` 為保留字。
_Avoid_: 專案資料夾、feature branch

**找碴**：
使用者看著地圖或 MVP 挑缺漏的迴圈——本方法的核心品管動作。想改就直接改場景檔、重畫地圖，不需要任何狀態或儀式。
_Avoid_: review（太泛）

## Relationships

- 一個 **需求資料夾** 恰有一組 **場景檔**，與其 **生成文件**
- **行為地圖** 與 spec 由 **場景檔** 單向重生；handoff 依 **場景檔** 與 **MVP** 現狀生成；**MVP** 是實作——依場景增量修改，不由場景全量重建
- **找碴** 發生在動手改 MVP 之前；改需求＝直接改 **場景檔** 再重畫地圖
