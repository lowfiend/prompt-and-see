# Changelog

## 0.5.0 — 2026-08-17

新增**施工端**：`/build-it` 領票施工主持人＋四支施工紀律模型零件。四支紀律由作者自用的 governance skills 去專案化、瘦身成單檔而來（砍平台對照檔與工具鏈綁定，宿主專案的 CLAUDE.md 與慣例檔為最終權威）。

- **`/build-it`**：一次一張票、只吃落檔——先以 git／tracker 判斷票的完成狀態（判斷不了就問），領 blockers 全完成、編號最前的票；場景即驗收：技術決定節定好的測試接縫上一場景一測試，測不了退手動演示；地基票以自身工程檢查（建置、煙霧測試）驗收；MVP 是畫面契約（可見行為，非實作手法）；慣例三層（既有事實照走 → `interviewing` 補問 → 定案落宿主慣例檔）；行為缺口停下指回 `/map-it`、技術決定衝突指回 `/to-spec`；驗收測試先紅後綠（既有實作已滿足時以暫時破壞反證）、測試資料自持；收尾全套測試＋code review（無工具則自審；修復宣稱須附對應測試或檢查結果，只列 diff 不算）＋commit 只 stage 本票變更
- **`architecture-governance`**：模組化是決定不是預設——強／弱／反訊號、shared／feature／app 分層責任、依賴方向鐵則、覆寫點 opt-in、過度設計自省清單
- **`ui-governance`**：語意變體按後果判定（primary／secondary／destructive）、token 管有語意的值（不准為湊 token 搬家魔數；選取類控件定義雙態）、重複兩處評估抽共用、只做本次行為所需的狀態且顏色非唯一訊號、固定 chrome 不遮內容、無障礙；brownfield 遷移時既有產品是綁定的 UI 契約
- **`i18n-governance`**：使用者可見文字不寫死、不串接組句（具名佔位符）、key 結構化命名且同義複用、敏感資訊遮罩、臨時 key 前綴標示且交付前清掉
- **`image-governance`**：檔名結構化 `[namespace]_[feature]_[element]_[state]`（小專案可省無區辨力的段）、狀態變體命名一致、通用資產通用命名、歸屬明確（未拆目錄由檔名前綴承載）、路徑不散落——多資產或整批替換才集中成對照表
- **`to-spec` 配套**：技術決定例示加「語言與框架（既有 repo 是事實不問）」；重生規則補「與環境事實衝突：事實照實更新，重新開放的取捨才訪談」；拆票按依賴序編號、地基票夠小就併入首張用到它的票、以工程檢查驗收

## 0.4.1 — 2026-08-14

補訪談盲區與落檔縫隙——加的是檢查角度，不是題庫。

- **`map-it`**：展開行為加三個檢查角度——動作的後果（通知、庫存回補這類事後感知）、列表基本面（排序、分頁、每筆顯示什麼）、操作顆粒度（整筆或部分）；訪談拍板的排除項寫進 feature description「不做」段——排除定案也是定案，只活在對話裡就會漏
- **`scenario-writing`**：feature description 除 WHY 外也收「不做」段，供下游 spec 收割
- **`to-spec`**：技術決定例示加「資料的時間語意（快照或連動）」；「不做什麼」明定收割來源含場景檔「不做」段
- **`to-handoff`**：「單向生成」明確為**管寫不管讀**——已知事項可取自當下對話與現狀，不限場景／地圖／MVP 三檔（原規則自打架：要寫「未實測」卻只准讀三檔）
- **`behavior-mapping`**：外部節點規則涵蓋雙向——以 Given 前提出現、或作為結果被導向（導去登入頁等）都畫虛線外部節點
- **`domain-language`（CONTEXT-FORMAT）**：詞條反例明文——只寫語意，不寫 UI 行為與狀態機，那是場景檔的事

## 0.3.1 — 2026-08-13

`to-spec` 砍掉轉接頭定位：不再替下游生成 SpecKit／OpenSpec／自家格式——RD 有自己跑順的規格工具，就直接拿場景檔當輸入，不需要 `to-spec`。

- **`to-spec` ＝ 極簡 spec**：生成四節式 `spec.md`——行為照搬場景（場景即行為描述與驗收標準）＋技術決定（依 `interviewing` 訪談技術負責人補齊，不在場則留選擇題 TODO；不寫檔案路徑與程式碼片段）＋拆票（垂直切片、每票標 Blocked by、大小以一個 context window 為度、場景即驗收、共同地基獨立成票）＋不做什麼（刻意排除的範圍，防施工腦補擴張）。重生時行為與拆票由場景重出，技術決定與不做什麼沿用已定案內容；訪談中冒出的新行為回 `/map-it`，不寫進 spec。拆票與「不做什麼」借鑑 Matt Pocock 的 `to-tickets`／`to-spec`
- **`to-handoff` RD 提示加一行紀律**：RD 用自己的 spec 工具時，行為以場景檔為準，工具只准補技術節，不准重新推導行為
- config 刪「下游規格格式」（三標題 → 兩標題）：目標格式不再是設定項

## 0.3.0 — 2026-08-12

簡化重設：刪除流程狀態機，回到「指令就是流程、對話確認就是授權、Git 管歷史」的最小工作流。

- **刪除凍結狀態機**：`狀態: 討論中／已凍結`、凍結日期、「凍結才准跑 to-*」前置全數移除。改需求＝再跑 `/map-it` 或直接下指令；動手改 MVP 前以「對話摘要＋使用者同意」為唯一授權——確認留在對話裡，不持久化成流程狀態
- **刪除 `/remap`**（併回 `/map-it`）與 `coverage.md`（含 SHA-256 新鮮度比對、交付閘門）：稽核交給 `map.md`＋Git diff，handoff 直接列本次對應的頁面與狀態
- **MVP 改為 code、不再是生成物**：免安裝、免編譯、直接打開的靜態 HTML（可多頁＋`assets/`，不需工具鏈）；有既有 MVP 就原地增量修改、無關功能保留，不標「手改無效」、不宣稱可由場景全量重建——上一版的收編／領土／接管／生成標記／防洗掉／全量重生協定隨之全數移除。repo 有正式產品 code 時只讀作參考，不修改、不宣稱等同
- **`map.md` 只剩圖**：檔頭帳目（狀態、關鍵決定、依賴、變更紀錄最近 5 筆）刪除，只留一行「由場景檔生成，手改無效」＋Mermaid
- **場景規則鬆綁**：廢除「每顆按鈕成功失敗成對」；有業務結果的操作寫成功結果，存在合理失敗／空資料／權限／邊界風險才補場景，純導覽不硬造失敗，不確定就問使用者。需求以使用者目標組織、場景可跨頁；WHY 訪談所得寫進 feature description，不另建決策紀錄
- **handoff／spec 無閘門**：使用者呼叫就依現狀生成；未完成、未驗證事項誠實列「已知事項」，不擋交付；版本歷史、manifest、可攜單包協定刪除
- config 縮為三標題（場景資料夾、場景語言、下游規格格式）；殼層來源、UI 設計 skill、交付模型移除；setup 非必須——缺 config 用預設值，能自己查到的不問使用者
- **MVP 位置固定 `behaviors/mvp/`、入口固定 `index.html`**：不可配置、不搜尋、不推測。使用者提供其他位置的靜態 HTML → 說明後放入 `behaviors/mvp/`；與既有內容實際衝突 → 列出衝突讓使用者決定保留、替換或合併；入口檔不同 → 問使用者以哪個為主、整理為 `index.html` 並修相對連結
- MVP 鷹架紀律：狀態切換是開發鷹架、不是產品 UI——與內容物理隔離、預設收合、明確標示「原型預覽・非正式 UI」，不內嵌在內容流裡冒充產品功能

## 0.2.0 — 2026-08-12

`to-mvp` 哲學轉向：可攜自足島 → 整合原型（脈絡＋標註）。

- `behaviors/mvp/`（新保留字，`map-it` 開 slug 時擋）＝整合原型：一畫面一頁面檔，每頁合併所有已凍結需求的行為（含失敗態）；共享 `assets/` 殼層（來源優先序：宿主 build 萃取——只承諾樣式保真＞UI 設計 skill＞設計系統文件＞中性；字型 self-host、禁 CDN）；入口頁 `behaviors/mvp/index.html` 由 `/to-mvp` 生成
- 廢除 `behaviors/<slug>/mvp/` 與單檔自足鐵律；`/to-mvp` 改為**全量重生**：每次讀取全部已凍結需求、重生整個原型——自然清掉改名頁、孤兒頁與手改；先在臨時目錄生成並驗證（節點覆蓋、失敗態可觸發、連結不斷），成功才替換、失敗保留舊原型；跨需求矛盾即停；檔名由畫面名稱轉 kebab-case、碰撞即停
- 每個已凍結需求落檔 `coverage.md`（地圖節點 × 頁面／狀態的人可讀表格；整檔覆蓋的當前快照、不累積歷史；檔頭逐檔記 `.feature` 的 SHA-256）；文案兩級：場景釘死照抄、未釘死補中性文案，要穩定就回場景釘死
- `to-handoff`：交付＝整合原型（脈絡）＋覆蓋對照表（標註）；可攜單包＝需求資料夾＋整個 `mvp/`（連結閉包天然成立），保留相對目錄層級；把關：來源 hash 不符或缺頁／無法觸發不准交付，未操作驗證與他需求缺口如實轉錄
- `behavior-mapping`：畫面名稱入 `CONTEXT.md` 詞彙表（頁的身分）；地圖改動＝原型待重生，`mvp/` 一律歸 `/to-mvp` 管；map 檔頭變更紀錄設上限（最近 5 筆，完整歷史交給 Git）、關鍵決定只列現行有效
- config 新增「殼層來源」「交付模型」（五標題 → 七標題）；設定檔固定住 `behaviors/config.md`，「場景資料夾」欄位決定實際根目錄

## 0.1.0 — 2026-08-11

首個版本。範圍：從一句需求到交付包。

- 10 個 skills：使用者呼叫 `setup-prompt-and-see`／`map-it`／`remap`／`to-spec`／`to-mvp`／`to-handoff`；模型呼叫 `interviewing`／`scenario-writing`／`behavior-mapping`／`domain-language`
- `behaviors/` 結構：純 slug 需求資料夾＝功能身分證；場景檔唯一手寫，其餘單向生成；`index.html` 總覽（按功能＋按畫面反查）
