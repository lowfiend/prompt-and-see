# prompt-and-see

## Language

**行為真相**：
一個功能「做出來總共有哪些行為」的唯一手寫紀錄——`behaviors/<slug>/*.feature` 場景檔。全 repo 只有它是手寫的。
_Avoid_: 規格、spec（那是下游翻譯件）、需求文件

**凍結**：
行為真相經找碴迴圈確認後的鎖定狀態，記於地圖檔頭。凍結是所有下游 `to-*` 動工的門檻；要改就 `/remap` 原地解凍再重新凍結。
_Avoid_: 定稿、final

**行為地圖**：
從場景檔單向生成的 Mermaid 圖（`map.md`）：畫面＝節點、動作＝邊、失敗標紅。功能的門面。
_Avoid_: 流程圖、線框圖

**產物**：
由行為真相單向生成的一切檔案（地圖、頁面檔、殼層、覆蓋對照表、handoff、spec、入口頁）。手改無效、過期即重生——過期的產物是謊言。
_Avoid_: 文件（不夠精確）

**需求資料夾**：
`behaviors/<slug>/`——一個功能的身分證（真相＋本功能的產物）。slug 一輩子不變；交付＝整合原型當脈絡＋本夾的覆蓋對照表當標註。
_Avoid_: 專案資料夾、feature branch

**整合原型**：
`behaviors/mvp/` 整夾——一個可走的原型，動工之前的「看見」發生在這裡。交付時它是「脈絡」，覆蓋對照表是「標註」。
_Avoid_: 自足島、單功能 MVP（舊制）

**頁面檔**：
整合原型裡的一個 HTML＝地圖上的一個畫面節點，合併所有已凍結需求在該畫面的行為（含失敗態）。檔名由畫面名稱轉成安全的 kebab-case；每次 `/to-mvp` 全量重生，手改會被下次生成覆蓋。
_Avoid_: 線框、mockup

**殼層**：
`behaviors/mvp/assets/`——整合原型共享的 CSS／JS／字型，生成物。來源優先序：宿主 build 產物萃取（樣式保真，元件行為仍由場景生成）＞設計系統生成＞中性樣式。
_Avoid_: 共用樣式表、theme

**入口頁**：
`behaviors/mvp/index.html`——整合原型的門面：按功能（狀態＋新鮮度）＋按畫面反查。
_Avoid_: 總覽頁（舊名）

**覆蓋對照表**：
`behaviors/<slug>/coverage.md`——地圖節點 × 頁面＋狀態的對照，缺漏一眼可見。每次 `/to-mvp` 整檔覆蓋的**當前快照**，不累積歷史。交付時的「標註」：告訴下游本需求動了整合原型的哪裡；`/to-handoff` 據以把關。
_Avoid_: 測試覆蓋率（不同概念）

**找碴**：
凍結前使用者看著地圖／整合原型挑缺漏的迴圈——本方法的核心品管動作。
_Avoid_: review（太泛）

## Relationships

- 一個 **需求資料夾** 恰有一份 **行為真相**，與多個 **產物**
- 一個 **頁面檔** 由所有涉及它的 **需求資料夾** 的真相合併生成；任一方重新凍結，原型即過期，下次 `/to-mvp` 全量重生
- **凍結** 是所有 `to-*` 的前置條件
- **找碴** 發生在 **凍結** 之前；凍結後發現缺漏走 `/remap` 回真相
